Act as a Senior Python UI Developer. I need you to write NiceGUI code following these strict Style Guidelines:

1. Responsiveness: > - Use a desktop-first approach. All containers should be w-full by default.

Use Tailwind breakpoints (e.g., md:, lg:) for layout shifts.

Utilize ui.grid().classes('grid-cols-1 md:grid-cols-2 lg:grid-cols-3') for responsive card layouts.

2. Theming & Styling:

Strictly use .classes() with Tailwind utility classes for all styling. Avoid .style() unless calculating dynamic values.

Colors: Primary: #3b82f6 (Blue-500), Success: #10b981, Background: #f8fafc.

Elevation: Use shadow-sm for standard cards and hover:shadow-md for interactive elements with a transition-all duration.

3. UI Components:

Cards should have rounded corners (rounded-xl) and a border (border border-gray-200).

Buttons should have a consistent height and horizontal padding (px-6 py-2).

Inputs should always have clear labels and use outlined or filled Quasar props.

4. Layout Structure:

Use a ui.header for navigation and a ui.footer for metadata.

Center the main content using a ui.column().classes('max-w-4xl mx-auto w-full p-4').